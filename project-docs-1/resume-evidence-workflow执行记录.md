# resume-evidence-workflow 执行记录

执行日期：2026-08-26

## 已完成

- 安装 `Elowwwen/resume-evidence-workflow` 到本地 Codex skills：`D:\Users\peiyaowan\.codex\skills\resume-evidence-workflow`。
- 读取并执行该 skill 的 Stage A：盘点旧简历、更新材料、项目文档和代码目录。
- 读取旧版 PDF 页面图像，校准原简历的基本信息、教育背景、项目结构和版式。
- 读取 `1.md` 中的新项目经历和实习经历。
- 读取两个项目目录中的 README、架构文档、流程文档、设计文档和源码检索结果，核对主要技术事实。
- 叠加 `industry-resume-toolkit` 的通用、大厂、开发岗规则，形成当前更适合 Java 后端/AI 应用后端方向的基础表达。

## 产出文件

- `经历库.md`：事实源、能力索引、claim ledger、责任边界、待确认问题。
- `基础简历.md`：通用基础版简历内容，不针对单个 JD。
- `简历版式档案.md`：旧版 PDF 的视觉结构和后续生成建议。

## 因缺少 JD 暂停的阶段

- Stage B：JD 解码和岗位要求映射。
- Stage C：针对具体 JD 选择最终改写 skill。
- Stage D：生成面向具体投递渠道的最终 Word/PDF/HTML 简历。
- Stage E：最终投递版被用户接受后的复用模块回写。

## 后续建议

拿到具体 JD 后，优先使用 `经历库.md` 做 requirement-to-evidence matrix，再从 `基础简历.md` 裁剪。当前最大待确认项是求职意向是否正式切换为 Java 后端开发，以及携程/博物馆项目中哪些动作可写“负责”而不是“参与”。
