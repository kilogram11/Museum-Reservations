# 基础简历

最后确认日期：2026-08-26

> 这是通用基础版本，不针对某一个 JD。后续投递时应从 `经历库.md` 取证据，再按岗位要求裁剪。

## 基本信息

- 姓名：万霈瑶
- 电话：18109070502
- 邮箱：18109070502@163.com
- 求职意向：Java 后端开发实习生 / 后端开发工程师（待确认；旧版 PDF 为“前端工程师”）

## 教育背景

**合肥工业大学 | 软件工程 | 本科**  
2023.09 - 2027.06

- 成绩：网站设计与开发技术 4.0/4.3，Python 语言与系统设计 4.0/4.3
- 主修课程：网站设计与开发技术、数据结构、软件安全、面向对象分析与设计、Python 语言与系统设计、Java 程序设计、互联网开发实训
- 荣誉/证书：CET-4、上海英语复旦社杯

## 实习经历

### 携程集团 | 大住宿事业群 | Java 后端开发实习生

2026.06 - 2026.08

业务介绍：参与酒店业务轻量级定时调度器建设，面向批量任务注册、按时触发、优先级执行、失败重试和运维观测场景，基于 Spring Boot + Trip FX + CDubbo 提供 SOA/REST 接入，底层使用 CRedis ZSET/HASH 构建应用内嵌式调度内核，支持 HTTP/gRPC 外调业务 Action。

- **调度内核建模**：参与设计 `timeline` + `executeline` 双队列模型，将“触发时间”和“执行优先级”解耦；基于 Redis ZSET score 分别维护任务到期时间与执行优先级，通过 TimeLineTrigger + TaskWorker 完成任务推进、消费和回写，支持 once、interval、cron、fixed_delay 4 类调度语义。
- **高并发推进与可靠性设计**：针对大量任务同时到期场景，使用 Redis `MULTI/EXEC` 保证 timeline 到 executeline 的状态迁移一致性；针对 fixed_delay 设计 timeout backup + 成功 callback 覆盖机制，确保 Action 失败时任务不会永久丢失，并可在兜底时间到期后重新进入调度。
- **性能验证与运维闭环**：在 2 核 4G FAT 环境完成压测，5 万个 once 任务同时到期可 45s 排空，消费吞吐超 1100 task/s，timeline/executeline 流转能力超 25000 task/s；建设任务注册、取消、查询、Dashboard、Monitor 等控制台能力，接口基准达到 registerTask / cancelTask 约 250/s、getTask 约 437/s，并沉淀 188 个自动化测试用例，其中约 110 个覆盖异常、边界、降级和并发场景。

## 项目经历

### 博物馆预约管理与 AI 助手平台

项目介绍：面向博物馆预约、核销、馆规咨询和运营管理的一体化平台，包含微信小程序、Vue 管理端与 Spring Boot 后端。用户侧支持场馆/活动浏览、预约下单、二维码入馆、预约记录、AI 咨询与文物识别；管理侧支持排期配置、预约核销、黑名单、公告、统计看板和 Excel 导出。

技术栈：Spring Boot 3、Java 17、MyBatis-Plus、MySQL、Redis、Lua、Spring AI、DeepSeek、Vue 3、微信小程序、ONNX Runtime。

- **AI 预约 Agent**：设计单 Agent + 双 ChatClient + 5 Tool 的预约助手，将预约查票/下单/取消与馆规问答拆分为不同链路；封装 `queryDays`、`queryTimes`、`submitBooking`、`listRecords`、`cancelBooking` 5 个 Tool，复用真实预约 Service，避免 AI 绕过业务校验直接读写数据。
- **RAG 与权限安全**：实现轻量级馆规 RAG，基于本地 384 维字符 n-gram 哈希向量和内存向量库检索公告/馆规，未命中返回固定兜底以降低编造风险；通过 `UserContext(ThreadLocal)` 绑定 JWT 登录态，AI 写 Tool 不接受外部 `userId`，防止越权预约/取消。
- **高并发库存一致性**：设计 Redis Lua 原子预扣 + MySQL 事务落单链路，使用 `booking:stock:{timeMark}` 管理时段库存、`booking:booked:{day}:{identityId}` 实现一证一约，MySQL 侧通过 `SUCC_CNT += delta` 原子记账，并在落单失败/取消预约时用 Lua 回补库存。
- **压测与业务闭环**：基于 JMeter 验证 80 并发抢 `LIMIT_CNT=50` 时成功 50 单且 0 超卖、同一身份 10 并发仅 1 单成功；实现二维码核销、预约消息、爽约扫描、自动拉黑/解禁和管理端统计看板，形成预约、履约、核销、风控完整闭环。

### 易宿酒店预订系统（跨端移动应用）

> 旧版简历项目。若后续投递 Java 后端岗，可压缩为“前端/跨端补充项目”；若投全栈或前端岗，可恢复为重点项目。

技术栈：Taro 4、React 18、Zustand、SCSS、Capacitor、Spring Boot 3。

- **状态流转与组件工程化**：引入轻量级 Zustand 管理跨页预订流转等核心状态；自研自适应 TabBar 与转场动画组件，提升多端渲染一致性与交互体验。
- **网络请求重构**：深度二次封装 API 请求，内置全局异常拦截、静默 Loading 与网络重试机制，实现 JWT 无感续签，提升接口安全性与用户体验。
- **多端架构落地**：基于 Taro 4 与 React 18 搭建底层架构，利用 Capacitor 实现 Web API 到 Android 原生的混合打包，并针对高版本系统 API 34+ 进行 WebView 兼容性调优。

## 专业技能

- **后端开发**：Java、Spring Boot、MyBatis-Plus、REST/SOA 接口设计、JWT 鉴权、业务异常处理。
- **数据库与缓存**：MySQL、Redis ZSET/HASH、Redis Lua、事务一致性、库存扣减、任务队列建模。
- **AI 应用工程**：Spring AI、OpenAI-compatible ChatClient、DeepSeek、Tool Calling、轻量 RAG、ONNX Runtime。
- **前端与跨端**：Vue 3、Vite、Element Plus、微信小程序、Taro 4、React 18、Zustand、Capacitor。
- **工程验证与协作**：JMeter 压测、自动化测试、CAT/TripLog、接口文档、项目设计文档。

## 个人优势

- 具备从前端/跨端到 Java 后端的完整项目经验，能理解用户端、管理端与服务端之间的接口和数据流转。
- 有高并发库存扣减、Redis 调度队列、AI Tool Calling、RAG 等工程化实践，能够把技术方案落到业务闭环与可验证指标上。
- 保留代码规范、文档沉淀和测试验证习惯，适合继续投递 Java 后端、AI 应用后端、业务系统开发相关岗位。

## 待确认后再投递

- 是否保留“求职意向”字段，以及最终写“Java 后端开发实习生”还是“后端开发工程师”。
- 是否保留旧版“易宿酒店预订系统”项目；若目标 JD 是纯后端，可以进一步压缩。
- 携程实习中“负责/参与/建设”的措辞强度需要按真实职责再确认。
