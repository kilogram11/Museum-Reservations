<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.museum.mapper.NewsMapper">
    <!-- ResultMap -->
    <resultMap id="BaseResultMap" type="com.museum.entity.News">
        <id column="_id" property="id" />
        <result column="NEWS_ID" property="newsId" />
        <result column="NEWS_TITLE" property="newsTitle" />
        <result column="NEWS_DESC" property="newsDesc" />
        <result column="NEWS_STATUS" property="newsStatus" />
        <result column="NEWS_VIEW_CNT" property="newsViewCnt" />
        <result column="NEWS_ADD_TIME" property="newsAddTime" />
        <result column="NEWS_EDIT_TIME" property="newsEditTime" />
        <result column="_pid" property="pid" />
        <result column="NEWS_ADD_IP" property="newsAddIp" />
        <result column="NEWS_EDIT_IP" property="newsEditIp" />
    </resultMap>

    <!-- Insert -->
    <!--
    <insert id="insert" parameterType="com.museum.entity.News">
        INSERT INTO news (
            _id, NEWS_ID, NEWS_TITLE, NEWS_DESC, NEWS_STATUS, NEWS_VIEW_CNT,
            NEWS_ADD_TIME, NEWS_EDIT_TIME, _pid, NEWS_ADD_IP, NEWS_EDIT_IP
        ) VALUES (
            #{id}, #{newsId}, #{newsTitle}, #{newsDesc}, #{newsStatus}, #{newsViewCnt},
            #{newsAddTime}, #{newsEditTime}, #{pid}, #{newsAddIp}, #{newsEditIp}
        )
    </insert>
    -->

    <!-- Select by ID -->
    <!--
    <select id="selectById" resultMap="BaseResultMap" parameterType="String">
        SELECT * FROM news WHERE _id = #{id}
    </select>
    -->

    <!-- Update by ID -->
    <!--
    <update id="updateById" parameterType="com.museum.entity.News">
        UPDATE news SET
            NEWS_ID = #{newsId},
            NEWS_TITLE = #{newsTitle},
            NEWS_DESC = #{newsDesc},
            NEWS_STATUS = #{newsStatus},
            NEWS_VIEW_CNT = #{newsViewCnt},
            NEWS_ADD_TIME = #{newsAddTime},
            NEWS_EDIT_TIME = #{newsEditTime},
            _pid = #{pid},
            NEWS_ADD_IP = #{newsAddIp},
            NEWS_EDIT_IP = #{newsEditIp}
        WHERE _id = #{id}
    </update>
    -->

    <!-- Delete by ID -->
    <!--
    <delete id="deleteById" parameterType="String">
        DELETE FROM news WHERE _id = #{id}
    </delete>
    -->
</mapper>
